{
  "name": "@karman/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Karman — Vite application shell (menu, scenes, HUD, rendering).",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@karman/core": "workspace:*"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  }
}

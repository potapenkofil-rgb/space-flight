/** Shared tuning constants for the flight scene. */

/** Camera-shake impulse on stage separation, screen px (PLAN.md §7 Agent D: "shake on separation"). */
export const STAGE_SEPARATION_SHAKE_PX = 9;
